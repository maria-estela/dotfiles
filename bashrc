export HISTSIZE=100000
export PYTHONSTARTUP=/home/francesco/.pystartup
alias lt="ls -lrt"
alias greps="grep --exclude-dir '.svn'"
#alias pyc="find -name '*.pyc' -o -name __pycache__ -o -name .coverage -o -name '*.egg-info' | xargs rm -rf -v"

# The editor invoked by C-xC-e (see man bash, edit-and-execute-command)
export VISUAL=emacs
