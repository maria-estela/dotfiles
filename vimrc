syntax off
set autoindent
set expandtab
set tabstop=4
set shiftwidth=4
set nowrap

" The following two commands allow to search case sensitive only when all
" letters are downcase
set ignorecase
set smartcase

set incsearch

" To store swap files into the same directory of edited file (to avoid name
" collisions for files with the same name
set dir=.
